#include "main.h"
/**
 * int _isupper - program that checks for uppercase character
 * @c - output to be extracted
 * Return: 0
 */
int _isupper(int c)
{
	if(c > "A" && c < "Z")
	{
	return(1);
	}
	else
	{
	return(0);
	}
}
