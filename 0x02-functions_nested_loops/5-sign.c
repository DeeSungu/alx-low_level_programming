#include "main.h"
/**
 * print_sign - a function that prints the sign of a number
 * Return: 0 or 1 or -1
 * @n: describes the output as integer
 */
int print_sign(int n)
{
int n = 0
	if (n > 0)
	{
		_putchar(+);
			return (1);
	}
	else
	{
		if (n <= 0)
		{
			putchar(-)
				return (-1);
		}
		else
		{
			putchar(0)
				return (0);
		}
	}
}
