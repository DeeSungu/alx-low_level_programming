#include "main.h"
/**
 * main - a program that checks for lwercase character
 * Return: 0
 * @c:the output of the program will be an integer
 */
int _islower(int c);
{
if (c >= 'a'; c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
