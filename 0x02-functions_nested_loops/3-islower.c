#include "main.h"
/**
 * _islower - a program that checks for lwercase character
 * Return: 0 or 1
 * @c:the output of the program will be an integer
 */
int _islower(int c)
{
if (c >= 'a'; c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
