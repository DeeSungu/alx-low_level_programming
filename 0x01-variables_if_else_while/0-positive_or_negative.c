#include <stdlib.h>
#include <time.h>
/** main - a program that prints output as positive or negative using the printf function
 * Return: 0
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */

	return (0);
}
